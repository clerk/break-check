---
---

The release workflow now force-moves the `v1` Action tag to the release commit
after each successful publish, replacing the manual step that drifted after
0.1.2. Workflow-only; no package change.
