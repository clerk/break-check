---
---

CI: add a concurrency group to the CI workflow (cancel superseded runs) and bump
the baseline workflow's checkout/setup-node/pnpm-action-setup pins to match the
rest of the repo. Tooling-only; no change to the published package.
