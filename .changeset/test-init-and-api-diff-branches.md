---
---

Tests: cover the `break-check init` command (default/`--force`/`-o`, refuse
existing) and the api-diff enum-value, readonly, and kind-switch breaking
branches. Test-only; no change to the published package.
