# CLAUDE.md

See @AGENTS.md for repo orientation, layout, build/test commands, conventions,
and subtleties. Treat that file as the source of truth; this file only exists
so Claude Code picks up the same guidance.
